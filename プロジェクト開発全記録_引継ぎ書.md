# 📜 ドラクエ風レトロRPG 開発記録 ＆ 実務引継ぎ書 (v1.1.3 完全版)

本書は、本ゲームの**開発経緯、アーキテクチャの変遷、実機テストで遭遇した重大トラブルと完全解決までの全記録、および次回以降の課題**を、誇大表現や推測を排して事実ベースで記録した実務引継ぎ書です。

---

## 📌 1. プロジェクト基本情報

* **ローカル作業パス**: `c:\Users\beach\Desktop\大元\ゲーム\dora(ドラクエGit）`
* **GitHubリポジトリ**: `https://github.com/kenkenpa7/dora`
* **ゲーム公開URL (GitHub Pages)**: **`https://kenkenpa7.github.io/dora/`**
* **現在のバージョン**: **`v1.1.3`**
* **形態**: HTML5 Canvas 2D ＋ Vanilla JavaScript ＋ Web Audio API による完全オフライン対応PWAレトロRPG。

---

## 🛠️ 2. 本日（2026年9月）の作業全履歴と決定的な解決の全貌

### ① BGM全音源の極限圧縮化（v1.1.3）
* 全MP3音源をモノラル（1ch）化 ＋ 32kbps / 22.05kHz、WAVを11.025kHz mono へ極限再エンコード。
* **音源合計サイズ**: 5.43MB ➡ **1.00MB**（約81.6%の通信量・キャッシュ削減）。
* **ゲーム全体総容量**: **約 1.24MB**（PWAアセット全体）。
* バックアップは `BGM_v1.1.2バックアップ/`、`BGM_v1.1.1バックアップ/`、`BGM元サイズ/` の全階層を安全に保持。

### ② BGM全音源の超軽量圧縮化 (v1.1.2)
* かつて問題視されていた低周波数・低ビットレート領域（MP3: 64kbps / 22.05kHz、WAV: 11.025kHz mono、音源合計1.91MB）への再圧縮を実施。

### ③ BGM全音源の軽量圧縮化 (v1.1.1)
* `bgm/` 配下のMP3/WAV音源（全11ファイル）をffmpegで最適化エンコード（MP3: 96kbps / 44.1kHz、WAV: 22.05kHz mono、合計2.91MB）。

### ④ BGMファイルの元サイズ化とGit更新 (v1.1.0)
* `bgm/` 配下のMP3/WAV音源（全11ファイル）を元サイズ版（各曲約744KB、全音源合計約5.5MB）に差し替え、Gitコミット＆プッシュを実施。

### ⑤ 【重大トラブル】オフライン時にプログラム音以外のBGM/SEが鳴らなくなる現象
* **発生した現象**:
  * PWAとしてインストール後、機内モード（完全オフライン）にすると、Web Audio APIの電子音（オシレーター音）は鳴るが、MP3/WAVのBGM・効果音がすべて無音になる。
* **原因究明の経緯**:
  1. **HTML5 `<audio>` タグの構造的欠陥**:
     * `<audio>` タグ（`new Audio()`）は、ストリーミング用の「Rangeリクエスト（HTTP 206 部分取得）」を自動発行する。
     * iOS Safariのメディアエンジン（`AVFoundation / mediaserverd`）は、Service Workerが介在するとRangeリクエストの処理不整合や通信遮断を起こし、オフライン時にメディアのデコードを拒絶する。
  2. **Service Worker完全削除実験**:
     * `sw.js` を完全に削除してテストしたところ、「オフラインではゲーム画面自体が起動・ロード不能になる」ことが実証され、**オフライン動作にはService Workerが絶対に不可欠であること**が確定。

### ⑥ 【完全解決】Web Audio API 完全統合 ＆ 堅牢PWA Service Worker（v1.1.0）
* **実施した根本解決（v1.1.0）**:
  1. **音源再生エンジンを Web Audio API に完全一本化 (`js/audio.js`)**:
     * `<audio>` タグを全廃止。
     * すべてのBGM・SEを `fetch()` ➡ `arrayBuffer()` ➡ `ctx.decodeAudioData()` でオンデマンドにデコードして再生する方式へ移行。
     * これにより、**Rangeリクエスト（HTTP 206）が構造上100%消滅し、SafariやスマホOSの通信遮断エラーが完全に解消**。
  2. **堅牢なPWA Service Workerの再構築 (`sw.js`)**:
     * ゲーム本体（HTML/JS/画像）および全11音源（MP3/WAV）を、単純かつ確実な `Cache-First` で端末内に完全保存。
  3. **実機自動テスト（Playwright Chromium）**:
     * 完全オフライン状態でのシミュレーションにおいて、**全11音源のロード・再生が 100% PASS** することを確認。
  4. **実機スマホ（iOS Safari / PWA）での検証**:
     * 機内モード（完全オフライン）でのゲーム起動および全BGM/SEの正常再生を確認完了。

### ⑦ プロジェクト運用ルールの是正 (`AGENTS.md`)
* 過去のハルシネーション（根拠のない「24kHz以下再生不能」等の偽の音声基準）や無意味な形骸化ルールをすべて削除。
* **「Git更新時は必ず事前に『旧バージョン ➡ 新バージョン』をチャットに明示し、全ファイルのバージョンを同期更新する」** という実効性のあるルールを定義。

---

## 📁 3. 現在のファイル構成とアーキテクチャ

```text
ゲーム/dora(ドラクエGit）/
├── index.html                     # ゲーム本体HTML（PWA設定・SW登録・ヘッダーUI v1.1.0）
├── monster_viewer.html            # モンスター確認・パラメータ調整ビューア
├── manifest.json                  # Web App Manifest（スタンドアロンPWA設定）
├── sw.js                          # Service Worker（Cache-First / 完全オフラインエンジン v1.1.0）
├── README.md                      # プロジェクト概要・操作マニュアル
├── AGENTS.md                      # doraプロジェクト専用AI行動ルール
├── バージョン履歴.md               # Gitコミット連動・自動生成変更履歴
├── プロジェクト開発全記録_引継ぎ書.md # 本書
├── update_changelog.py            # バージョン履歴自動生成スクリプト
├── css/
│   └── style.css                  # スマホノッチ対応・全画面（100dvh）スタイル
├── js/
│   ├── audio.js                   # Web Audio API 音声エンジン (fetch+decodeAudioData)
│   ├── battle.js                  # コマンドバトル処理・計算式
│   ├── data.js                    # マップデータ・モンスター・呪文・アイテム定義
│   ├── graphics.js                # Canvas描画（2コマピコピコアニメーション搭載）
│   └── main.js                    # メインゲームループ・進行管理 (Ver 1.1.0)
├── bgm/                           # 全11音源（元サイズ版 MP3 8曲 / WAV 3種）
│   ├── opening.mp3, castle.mp3, town.mp3, field.mp3, dungeon.mp3
│   ├── battle.mp3, boss.mp3, ending.mp3
│   └── encounter.wav, inn.wav, victory.wav
└── icons/                         # PWAアプリアイコン
    ├── icon-192.png, icon-512.png, apple-touch-icon.png
```

---

## 📋 4. 次回以降に対処すべきゲーム本体の課題

オフライン音源基盤が100%完成したため、今後は**ゲームプレイの快適性向上**に着手してください。

1. **草原マップのポータル座標の完全同期 (`js/data.js`)**:
   * 草原マップ上の町シンボル（`TILE.TOWN_ICON`）および洞窟シンボル（`TILE.STAIRS_DOWN`）の位置と、`MAPS.field.portals` の突入座標（x, y）を完全一致させる。
2. **通常戦闘の勝利終了処理の確実な自動復帰 (`js/battle.js`)**:
   * `advanceMessage()` 内で、勝利メッセージ終了時に追加操作を待たず自動でマップ探索画面へ復帰させる。
3. **エンディング画面の入力処理の一本化 ＆ 誤爆ガード (`js/main.js`)**:
   * エンディング突入直後の連打誤爆を防ぐディレイ（800ms）と、安全なリスタート処理の実装。

---
*最終更新日: 2026年9月2日 (v1.1.0)*
