# Agents (doraプロジェクト個別設定)

## 1. バージョン管理・Git更新ルール（強制プロセス）

*   **Git更新時のバージョン連動チェック:**
    - **[条件]** doraプロジェクト内でプログラム、音源・画像アセット、スタイル、設定ファイルを変更し、Gitコミットまたはプッシュを行う場合。
    - **[強制手順]**
      1. 単なるファイルコミットで終わらせず、必ずバージョン（例: `v1.0.6` ➡ `v1.0.7`）の更新をセットで実施せよ。
      2. コマンド実行前のチャット事前提示において、必ず **「更新前バージョン ➡ 更新後バージョン（例: `v1.0.6` ➡ `v1.0.7` / キャッシュ `v14` ➡ `v15`）」** を明示せよ。
      3. 以下の3箇所のバージョン表記・キャッシュ番号を同期して書き換えること：
         - `index.html`: ヘッダーのバージョン表記（例: `v1.0.7`）
         - `js/main.js`: タイトル画面のバージョンプロパティ（例: `this.version = 'Ver 1.0.7'`）
         - `sw.js`: Service Workerのキャッシュ名（例: `CACHE_NAME = 'dora-gh-pages-v15'`）
      4. コミットメッセージにはバージョン番号を明記すること（例: `git commit -m "update: 音源更新に伴いバージョンを v1.0.7 に更新"`）。
      5. コミット後、自動更新された `バージョン履歴.md` を確認し、未追跡のゴミファイルが混入していないことを検証すること。

*   **Gitステージングの範囲限定:**
    - **[条件]** `git add` を実行する場合。
    - **[強制手順]**
      - `git add .` や `git add -A` の全指定は禁止。必ず変更した対象ファイル/フォルダ（例: `git add bgm/` や `git add index.html js/main.js sw.js`）を明示して個別指定すること。
      - バックアップ用フォルダ（`BGM元サイズ/`、`BGM_v1.1.1バックアップ/`、`BGM_v1.1.2バックアップ/` 等）などの未追跡ファイルを誤ってGitに巻き込まないこと。

## 2. 音源アセット（BGM/SE）の標準圧縮規格・追加更新ルール（デフォルト設定・強制）

今後、音源の新規追加・差し替え・更新を行う場合は、**以下の規格を本プロジェクトの「絶対的デフォルト設定」として強制適用**せよ。

*   **音源標準圧縮規格**:
    - **BGM (MP3)**: **モノラル (`-ac 1`) ＋ `32 kbps` (`-b:a 32k`) ＋ `22.05 kHz` (`-ar 22050`)**
    - **効果音 (WAV)**: **モノラル (`-ac 1`) ＋ `11.025 kHz` (`-ar 11025`) ＋ `16-bit PCM`**
*   **音源追加・変更時の必須手順**:
    1. 元の最高音質音源は必ず `BGM元サイズ/` フォルダ内に元ファイル名で保管すること。
    2. `bgm/` フォルダへ配置する音源は、必ず上記標準規格（`python compress_bgm.py`）で圧縮したもののみとすること。
    3. 新曲・新SEを追加した際は、以下のファイルを同期更新すること：
       - `sw.js`: `ASSETS_TO_CACHE` 配列にパスを追記し、`CACHE_NAME` を繰り上げ。
       - `js/audio.js`: 音源キーとパスを登録。
       - `index.html` / `js/main.js`: バージョン表記を同期繰り上げ。
